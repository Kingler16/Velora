# Drop-in: Claude-CLI Credentials liegen unter /home/admin/.claude/.
# Service läuft als User=root → ohne HOME-Override sucht CLI in /root/.claude → "Not logged in".
[Service]
Environment=HOME=/home/admin
